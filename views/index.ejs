<!DOCTYPE html>
<html>
<% include ./partials/head %>
<body>
<% include ./partials/topbar %>
<% include ./partials/navbar %>
<div class="greeting">
  <h2>
  <% if(user){ %>
    Welcome, <%= user.fname %> <%= user.lname %>
  <% } else { %>
    Welcome, Guest
  <% } %>
  </h2>
</div>
</body>
</html>
